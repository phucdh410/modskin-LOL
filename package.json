{
  "name": "app",
  "version": "1.0.0",
  "main": "run-electron.js",
  "scripts": {
    "start": "electron .",
    "build:exe": "npx electron-packager . App --platform=win32 --arch=x64 --overwrite"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cheerio": "^1.0.0",
    "copy-paste": "^1.5.3",
    "puppeteer": "^23.4.1"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.5",
    "electron": "^32.1.2"
  }
}
